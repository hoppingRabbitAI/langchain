# 需求功能点

1. 口型同步
2. 文生视频
3. 图生视频
4. 多图生视频
5. 动作控制
6. 视频延长
7. 图像生成
8. Omni-image



------

# 对口型服务 API 文档

## 1. 人脸识别 (Identify Face)

用于识别视频中的人脸，获取 `face_id` 及可进行对口型的时间区间。

- **请求地址**: `POST /v1/videos/identify-face`
- **请求/响应格式**: `application/json`

### 请求头

| **字段**      | **值**           | **描述**         |
| ------------- | ---------------- | ---------------- |
| Content-Type  | application/json | 数据交换格式     |
| Authorization | 鉴权信息         | 参考接口鉴权文档 |

### 请求体

| **字段**    | **类型** | **必填** | **默认值** | **描述**                                                     |
| ----------- | -------- | -------- | ---------- | ------------------------------------------------------------ |
| `video_id`  | string   | 可选     | 无         | 可灵AI生成的视频ID。与 `video_url` 二选一。支持30天内生成、时长 ≤ 60s 的视频。 |
| `video_url` | string   | 可选     | 无         | 上传视频的 URL。支持 .mp4/.mov，大小 ≤ 100MB，时长 2s-60s，分辨率 720p/1080p，边长 512px-2160px。 |

### 响应体

JSON

```
{
  "code": 0,
  "message": "string",
  "request_id": "string",
  "data": {
    "session_id": "id", // 会话ID，有效期24小时
    "face_data": [
      {
        "face_id": "string", // 视频中的人脸ID
        "face_image": "url", // 人脸截图示意图
        "start_time": 0,    // 可对口型区间起点 (ms)
        "end_time": 5200    // 可对口型区间终点 (ms)
      }
    ]
  }
}
```

------

## 2. 创建任务 (Advanced Lip Sync)

基于识别出的人脸 ID 和音频文件创建对口型任务。

- **请求地址**: `POST /v1/videos/advanced-lip-sync`
- **请求/响应格式**: `application/json`

### 请求体参数详情

| **字段**                            | **类型** | **必填** | **描述**                                                     |
| ----------------------------------- | -------- | -------- | ------------------------------------------------------------ |
| `session_id`                        | string   | 是       | 人脸识别接口返回的会话 ID                                    |
| `face_choose`                       | object[] | 是       | 指定人脸对口型配置（目前仅支持单人）                         |
| `face_choose.face_id`               | string   | 是       | 人脸识别接口返回的人脸 ID                                    |
| `face_choose.audio_id`              | string   | 可选     | 试听接口生成的音频 ID（时长 2s-60s）。与 `sound_file` 二选一 |
| `face_choose.sound_file`            | string   | 可选     | 音频 Base64 或 URL。支持 .mp3/.wav/.m4a，大小 ≤ 5MB          |
| `face_choose.sound_start_time`      | long     | 是       | 音频裁剪起点 (ms)                                            |
| `face_choose.sound_end_time`        | long     | 是       | 音频裁剪终点 (ms)                                            |
| `face_choose.sound_insert_time`     | long     | 是       | 裁剪后音频在视频中的插入起始点 (ms)                          |
| `face_choose.sound_volume`          | float    | 否       | 音频音量 [0, 2]，默认 1                                      |
| `face_choose.original_audio_volume` | float    | 否       | 原视频音量 [0, 2]，默认 1                                    |
| `external_task_id`                  | string   | 否       | 自定义任务 ID，需保证唯一性                                  |
| `callback_url`                      | string   | 否       | 任务状态变更时的回调地址                                     |

### 响应体

JSON

```
{
  "code": 0,
  "message": "string",
  "request_id": "string",
  "data": {
    "task_id": "string",
    "task_info": {
      "external_task_id": "string"
    },
    "task_status": "submitted", // 枚举: submitted, processing, succeed, failed
    "created_at": 1722769557708,
    "updated_at": 1722769557708
  }
}
```

------

## 3. 查询任务（单个）

通过 `task_id` 查询特定对口型任务的状态和结果。

- **请求地址**: `GET /v1/videos/advanced-lip-sync/{task_id}`
- **请求路径参数**:
  - `task_id`: 必须，创建任务时返回的任务 ID。

### 响应体

JSON

```
{
  "code": 0,
  "message": "string",
  "request_id": "string",
  "data": {
    "task_id": "string",
    "task_status": "succeed",
    "task_status_msg": "string", // 失败时展示原因
    "task_info": {
      "parent_video": {
        "id": "string",
        "url": "string",
        "duration": "string"
      }
    },
    "task_result": {
      "videos": [
        {
          "id": "string",
          "url": "string", // 对口型视频URL，有效期30天
          "duration": "string"
        }
      ]
    },
    "created_at": 1722769557708,
    "updated_at": 1722769557708
  }
}
```

------

## 4. 查询任务（列表）

分页查询当前用户下的对口型任务列表。

- **请求地址**: `GET /v1/videos/advanced-lip-sync`
- **查询参数**:
  - `pageNum`: 页码，范围 [1, 1000]，默认 1。
  - `pageSize`: 每页数量，范围 [1, 500]，默认 30。

### 响应体

JSON

```
{
  "code": 0,
  "message": "string",
  "request_id": "string",
  "data": [
    {
      "task_id": "string",
      "task_status": "succeed",
      "task_status_msg": "string",
      "task_info": { ... },
      "task_result": {
        "videos": [ { "id": "string", "url": "string", "duration": "string" } ]
      },
      "created_at": 1722769557708,
      "updated_at": 1722769557708
    }
  ]
}
```



------

# 文生视频 (Text to Video) API 文档

## 1. 创建任务

使用文本提示词生成视频，支持模型版本切换、运镜控制及多种比例。

- **请求地址**: `POST /v1/videos/text2video`
- **请求/响应格式**: `application/json`

### 请求头

| **字段**      | **值**           | **描述**         |
| ------------- | ---------------- | ---------------- |
| Content-Type  | application/json | 数据交换格式     |
| Authorization | 鉴权信息         | 参考接口鉴权文档 |

### 请求体

| **字段**           | **类型** | **必填** | **默认值** | **描述**                                                     |
| ------------------ | -------- | -------- | ---------- | ------------------------------------------------------------ |
| `model_name`       | string   | 否       | kling-v1   | 模型版本。可选：`kling-v1`, `kling-v1-6`, `kling-v2-master` 等 |
| `prompt`           | string   | **是**   | 无         | 正向提示词，不超过 2500 字符                                 |
| `negative_prompt`  | string   | 否       | 空         | 负向提示词，不超过 2500 字符                                 |
| `sound`            | string   | 否       | off        | 是否生成声音（`on`/`off`）。仅 V2.6+ 支持                    |
| `cfg_scale`        | float    | 否       | 0.5        | 提示词相关性 [0, 1]。V2.x 不支持此参数                       |
| `mode`             | string   | 否       | std        | 生成模式。`std`: 标准, `pro`: 高品质专家模式                 |
| `aspect_ratio`     | string   | 否       | 16:9       | 画面纵横比。可选：`16:9`, `9:16`, `1:1`                      |
| `duration`         | string   | 否       | 5          | 视频时长（秒）。可选：`5`, `10`                              |
| `camera_control`   | object   | 否       | 无         | 运镜控制协议                                                 |
| `external_task_id` | string   | 否       | 无         | 用户自定义任务ID，需保证唯一性                               |
| `callback_url`     | string   | 否       | 无         | 任务状态变更时的回调通知地址                                 |

#### 运镜控制 (`camera_control`) 详情

- **type**: 运镜类型。可选：`simple`, `down_back`, `forward_up`, `right_turn_forward`, `left_turn_forward`
- **config**: 当 `type` 为 `simple` 时必填。以下 6 个参数选其一填值（其余须为 0），范围均为 `[-10, 10]`：
  - `horizontal`: 水平平移（负左正右）
  - `vertical`: 垂直平移（负下正上）
  - `pan`: 水平摇镜（绕y轴旋转）
  - `tilt`: 垂直摇镜（沿x轴旋转）
  - `roll`: 旋转运镜（绕z轴旋转）
  - `zoom`: 变焦（负长焦/窄视野，正短焦/广视野）

### 响应体

JSON

```
{
  "code": 0,
  "message": "string",
  "request_id": "string",
  "data": {
    "task_id": "string",
    "task_info": { "external_task_id": "string" },
    "task_status": "submitted", // 枚举：submitted, processing, succeed, failed
    "created_at": 1722769557708,
    "updated_at": 1722769557708
  }
}
```

------

## 2. 查询任务（单个）

根据任务 ID 或自定义 ID 获取生成结果。

- **请求地址**: `GET /v1/videos/text2video/{id}`
- **路径参数**:
  - `task_id`: 路径中的 `{id}` 可直接填写系统生成的 `task_id`。
  - 或通过 `external_task_id` 查询（二选一）。

### 响应体

JSON

```
{
  "code": 0,
  "message": "string",
  "request_id": "string",
  "data": {
    "task_id": "string",
    "task_status": "succeed",
    "task_status_msg": "string", // 失败原因
    "task_info": { "external_task_id": "string" },
    "task_result": {
      "videos": [
        {
          "id": "string",
          "url": "string", // 视频下载地址，有效期30天
          "duration": "string"
        }
      ]
    },
    "created_at": 1722769557708,
    "updated_at": 1722769557708
  }
}
```

------

## 3. 查询任务（列表）

批量获取任务状态。

- **请求地址**: `GET /v1/videos/text2video`
- **查询参数**:
  - `pageNum`: 页码，范围 [1, 1000]，默认 1。
  - `pageSize`: 每页数据量，范围 [1, 500]，默认 30。

### 响应体

JSON

```
{
  "code": 0,
  "message": "string",
  "request_id": "string",
  "data": [
    {
      "task_id": "string",
      "task_status": "succeed",
      "task_result": {
        "videos": [ { "id": "string", "url": "string", "duration": "string" } ]
      },
      "created_at": 1722769557708,
      "updated_at": 1722769557708
    }
  ]
}
```



------

# 多图参考生视频 (Multi-Image to Video) API 文档

## 1. 创建任务

通过上传多张参考图（最多 4 张）来生成视频，适用于需要更高一致性或复杂参考的场景。

- **网络协议**: `HTTPS`
- **请求地址**: `POST /v1/videos/multi-image2video`
- **请求/响应格式**: `application/json`

### 请求头

| **字段**      | **值**           | **描述**         |
| ------------- | ---------------- | ---------------- |
| Content-Type  | application/json | 数据交换格式     |
| Authorization | 鉴权信息         | 参考接口鉴权文档 |

### 请求体参数

| **字段**           | **类型** | **必填** | **默认值** | **描述**                                      |
| ------------------ | -------- | -------- | ---------- | --------------------------------------------- |
| `model_name`       | string   | 否       | kling-v1-6 | 模型版本，目前仅支持 `kling-v1-6`             |
| `image_list`       | array    | **是**   | 无         | 图片列表，包含 1-4 张图片。支持 URL 或 Base64 |
| `prompt`           | string   | **是**   | 无         | 正向提示词，不超过 2500 字符                  |
| `negative_prompt`  | string   | 否       | 空         | 负向提示词                                    |
| `mode`             | string   | 否       | std        | 生成模式：`std` (标准), `pro` (专家)          |
| `duration`         | string   | 否       | 5          | 视频时长（秒）：`5`, `10`                     |
| `aspect_ratio`     | string   | 否       | 16:9       | 画面纵横比：`16:9`, `9:16`, `1:1`             |
| `external_task_id` | string   | 否       | 无         | 用户自定义任务 ID，需保证唯一性               |
| `callback_url`     | string   | 否       | 无         | 任务状态变更时的回调通知地址                  |

> **⚠️ 注意事项：**
>
> 1. **Base64 格式**：请直接提供编码后的字符串，**严禁**包含前缀（如 `data:image/png;base64,`）。
> 2. **图片限制**：大小 ≤ 10MB，宽高 ≥ 300px，比例介于 1:2.5 ~ 2.5:1 之间。
> 3. **无裁剪逻辑**：系统不会自动裁剪图片，请在上传前确保主体已选择好。

------

## 2. 查询任务（单个）

- **请求地址**: `GET /v1/videos/multi-image2video/{id}`
- **说明**: `{id}` 处可填写 `task_id` 或 `external_task_id`。

### 响应体

JSON

```
{
  "code": 0,
  "message": "string",
  "request_id": "string",
  "data": {
    "task_id": "string",
    "task_status": "succeed", // 枚举: submitted, processing, succeed, failed
    "task_status_msg": "string", // 失败原因描述
    "task_info": { "external_task_id": "string" },
    "task_result": {
      "videos": [
        {
          "id": "string",
          "url": "string", // 视频链接，有效期30天
          "duration": "string"
        }
      ]
    },
    "created_at": 1722769557708,
    "updated_at": 1722769557708
  }
}
```

------

## 3. 查询任务（列表）

- **请求地址**: `GET /v1/videos/multi-image2video`
- **查询参数**:
  - `pageNum`: 页码 [1, 1000]，默认 1
  - `pageSize`: 每页数量 [1, 500]，默认 30

### 响应体

JSON

```
{
  "code": 0,
  "message": "string",
  "data": [
    {
      "task_id": "string",
      "task_status": "succeed",
      "task_info": { "external_task_id": "string" },
      "task_result": {
        "videos": [{ "url": "...", "duration": "5" }]
      },
      "created_at": 1722769557708,
      "updated_at": 1722769557708
    }
  ]
}
```



------

# 动作控制 (Motion Control) API 文档

## 1. 创建任务

通过一张参考图（角色）和一段动作视频（动作源）生成视频，使图片中的角色执行视频中的动作。

- **网络协议**: `HTTPS`
- **请求地址**: `POST /v1/videos/motion-control`
- **请求/响应格式**: `application/json`

### 请求头

| **字段**      | **值**           | **描述**         |
| ------------- | ---------------- | ---------------- |
| Content-Type  | application/json | 数据交换格式     |
| Authorization | 鉴权信息         | 参考接口鉴权文档 |

### 请求体参数

| **字段**                | **类型** | **必填** | **默认值** | **描述**                                                     |
| ----------------------- | -------- | -------- | ---------- | ------------------------------------------------------------ |
| `image_url`             | string   | **是**   | 无         | **参考图像**。URL 或 Base64（不可带 data: 前缀）。人物需清晰，比例尽量与动作视频一致。 |
| `video_url`             | string   | **是**   | 无         | **动作视频 URL**。建议 1 人动作，一镜到底，避免切镜。支持 .mp4/.mov。 |
| `character_orientation` | string   | **是**   | 无         | **人物朝向**。`image`: 与图片一致（动作视频限 10s）；`video`: 与视频一致（动作视频限 30s）。 |
| `mode`                  | string   | **是**   | 无         | **生成模式**：`std` (标准), `pro` (专家/高品质)。            |
| `prompt`                | string   | 否       | 空         | 文本提示词，可增加画面元素或运镜效果。不超过 2500 字符。     |
| `keep_original_sound`   | string   | 否       | yes        | 是否保留视频原声：`yes`, `no`。                              |
| `external_task_id`      | string   | 否       | 无         | 自定义任务 ID，需保证唯一性。                                |
| `callback_url`          | string   | 否       | 无         | 任务状态变更时的回调通知地址。                               |

> **💡 关键指南：**
>
> - **角色要求**：建议上半身或全身肢体清晰，避免倒立、平卧或比例过小。
> - **视频要求**：边长在 340px~3850px 之间，大小 ≤ 100MB，时长 ≥ 3s。
> - **自动截取**：若动作难度极高或过快，模型可能仅提取部分有效连续动作，导致生成时长短于上传时长。

### 响应体

JSON

```
{
  "code": 0,
  "message": "string",
  "request_id": "string",
  "data": {
    "task_id": "string",
    "task_info": { "external_task_id": "string" },
    "task_status": "submitted", // 枚举：submitted, processing, succeed, failed
    "created_at": 1722769557708,
    "updated_at": 1722769557708
  }
}
```

------

## 2. 查询任务（单个）

- **请求地址**: `GET /v1/videos/motion-control/{task_id}`

### 响应体

JSON

```
{
  "code": 0,
  "message": "string",
  "request_id": "string",
  "data": {
    "task_id": "string",
    "task_status": "succeed",
    "task_status_msg": "string", // 失败原因
    "task_info": { "external_task_id": "string" },
    "task_result": {
      "videos": [
        {
          "id": "string",
          "url": "string", // 视频 URL，有效期 30 天
          "duration": "string"
        }
      ]
    },
    "created_at": 1722769557708,
    "updated_at": 1722769557708
  }
}
```

------

## 3. 查询任务（列表）

- **请求地址**: `GET /v1/videos/motion-control`
- **参数**: `pageNum` (默认1), `pageSize` (默认30)

### 响应体

JSON

```
{
  "code": 0,
  "message": "string",
  "data": [
    {
      "task_id": "string",
      "task_status": "succeed",
      "task_result": {
        "videos": [{ "url": "...", "duration": "5" }]
      },
      "created_at": 1722769557708
    }
  ]
}
```



------

# 视频延长 (Video Extend) API 文档

> **核心提示：**
>
> - **功能说明**：视频延长是对文生或图生视频的续写。单次可延长 **4~5s**。
> - **限制条件**：模型版本和模式自动继承源视频，不可更改。总视频时长上限为 **3分钟**。
> - **有效期**：源视频生成超过 30 天后会被清理，届时将无法进行延长。

## 1. 创建任务

- **网络协议**: `HTTPS`
- **请求地址**: `POST /v1/videos/video-extend`
- **请求/响应格式**: `application/json`

### 请求头

| **字段**      | **值**           | **描述**         |
| ------------- | ---------------- | ---------------- |
| Content-Type  | application/json | 数据交换格式     |
| Authorization | 鉴权信息         | 参考接口鉴权文档 |

### 请求体

| **字段**          | **类型** | **必填** | **默认值** | **描述**                                             |
| ----------------- | -------- | -------- | ---------- | ---------------------------------------------------- |
| `video_id`        | string   | **是**   | 无         | 待延长的视频 ID。支持文生、图生或已延长过的视频。    |
| `prompt`          | string   | 否       | 无         | 续写部分的正向提示词，不超过 2500 字符。             |
| `negative_prompt` | string   | 否       | 无         | 续写部分的负向提示词。                               |
| `cfg_scale`       | float    | 否       | 0.5        | 提示词参考强度 [0, 1]，数值越大，结果越符合 prompt。 |
| `callback_url`    | string   | 否       | 无         | 任务状态变更时的回调地址。                           |

### 响应体

JSON

```
{
  "code": 0,
  "message": "string",
  "request_id": "string",
  "data": {
    "task_id": "string",
    "task_status": "submitted", // 枚举：submitted, processing, succeed, failed
    "created_at": 1722769557708,
    "updated_at": 1722769557708
  }
}
```

------

## 2. 查询任务（单个）

- **请求地址**: `GET /v1/videos/video-extend/{task_id}`

### 响应体核心字段

- `task_info.parent_video`: 包含续写前原视频的 ID、URL 及时长。
- `task_result.videos`: 包含**续写后完整视频**的信息。

JSON

```
{
  "code": 0,
  "data": {
    "task_id": "string",
    "task_status": "succeed",
    "task_info": {
      "parent_video": {
        "id": "string",
        "url": "string",
        "duration": "string"
      }
    },
    "task_result": {
      "videos": [
        {
          "id": "string", // 延长后的新视频ID
          "url": "string", // 延长后的完整视频URL
          "duration": "string" // 总时长 (原时长 + 约5s)
        }
      ]
    }
  }
}
```

------

## 3. 查询任务（列表）

- **请求地址**: `GET /v1/videos/video-extend`
- **查询参数**: `pageNum` (默认1), `pageSize` (默认30)

### 响应体

JSON

```
{
  "code": 0,
  "message": "string",
  "data": [
    {
      "task_id": "string",
      "task_status": "succeed",
      "task_info": { "parent_video": { ... } },
      "task_result": { "videos": [ { ... } ] },
      "created_at": 1722769557708,
      "updated_at": 1722769557708
    }
  ]
}
```



------

# 图像生成 (Image Generation) API 文档

## 1. 创建任务

支持文生图和图生图。在使用图生图（参考图）时，可指定角色特征或人脸相似度参考。

- **网络协议**: `HTTPS`
- **请求地址**: `POST /v1/images/generations`
- **请求/响应格式**: `application/json`

### 请求头

| **字段**      | **值**           | **描述**         |
| ------------- | ---------------- | ---------------- |
| Content-Type  | application/json | 数据交换格式     |
| Authorization | 鉴权信息         | 参考接口鉴权文档 |

### 请求体参数

| **字段**          | **类型** | **必填** | **默认值** | **描述**                                                     |
| ----------------- | -------- | -------- | ---------- | ------------------------------------------------------------ |
| `model_name`      | string   | 否       | kling-v1   | 模型版本。枚举值：`kling-v1`, `kling-v1-5`, `kling-v2`, `kling-v2-1` 等 |
| `prompt`          | string   | **是**   | 无         | 正向提示词，不超过 2500 字符                                 |
| `negative_prompt` | string   | 否       | 空         | 负向提示词（**注：图生图场景下不支持**）                     |
| `image`           | string   | 否       | 空         | 参考图像。支持 URL 或 Base64（**严禁带 data: 前缀**）        |
| `image_reference` | string   | 否*      | 无         | 参考类型：`subject` (角色特征), `face` (人物长相)。**使用 v1-5 且有参考图时必填** |
| `image_fidelity`  | float    | 否       | 0.5        | 图片参考强度 [0, 1]                                          |
| `human_fidelity`  | float    | 否       | 0.45       | 五官相似度参考强度 [0, 1]（仅 `subject` 类型有效）           |
| `resolution`      | string   | 否       | 1k         | 清晰度：`1k` (标清), `2k` (高清)                             |
| `aspect_ratio`    | string   | 否       | 16:9       | 纵横比：`16:9`, `9:16`, `1:1`, `4:3`, `3:4`, `3:2`, `2:3`, `21:9` |
| `n`               | int      | 否       | 1          | 生成图片数量 [1, 9]                                          |
| `callback_url`    | string   | 否       | 无         | 任务状态变更时的回调通知地址                                 |

------

## 2. 查询任务（单个）

- **请求地址**: `GET /v1/images/generations/{task_id}`

### 响应体

JSON

```
{
  "code": 0,
  "message": "string",
  "request_id": "string",
  "data": {
    "task_id": "string",
    "task_status": "succeed", // submitted, processing, succeed, failed
    "task_status_msg": "string", // 失败原因描述
    "created_at": 1722769557708,
    "updated_at": 1722769557708,
    "task_result": {
      "images": [
        {
          "index": 0,
          "url": "https://..." // 生成图片的 URL，有效期 30 天
        }
      ]
    }
  }
}
```

------

## 3. 查询任务（列表）

- **请求地址**: `GET /v1/images/generations`
- **查询参数**:
  - `pageNum`: 页码 [1, 1000]，默认 1
  - `pageSize`: 每页数量 [1, 500]，默认 30

### 响应体

JSON

```
{
  "code": 0,
  "message": "string",
  "data": [
    {
      "task_id": "string",
      "task_status": "succeed",
      "task_result": {
        "images": [
          { "index": 0, "url": "..." }
        ]
      },
      "created_at": 1722769557708,
      "updated_at": 1722769557708
    }
  ]
}
```



------

# Omni-Image (O1) 图像生成 API 文档

## 1. 创建任务

Omni 模型支持多图参考、主体库参考以及高度灵活的文本引导。

- **网络协议**: `HTTPS`
- **请求地址**: `POST /v1/images/omni-image`
- **请求/响应格式**: `application/json`

### 请求头

| **字段**      | **值**           | **描述**         |
| ------------- | ---------------- | ---------------- |
| Content-Type  | application/json | 数据交换格式     |
| Authorization | 鉴权信息         | 参考接口鉴权文档 |

### 请求体参数

| **字段**           | **类型** | **必填** | **默认值**     | **描述**                                                     |
| ------------------ | -------- | -------- | -------------- | ------------------------------------------------------------ |
| `model_name`       | string   | 否       | kling-image-o1 | 模型版本，枚举值：`kling-image-o1`                           |
| `prompt`           | string   | **是**   | 无             | 文本提示词。支持使用 `<<<image_1>>>` 格式引用 `image_list` 中的图片 |
| `image_list`       | array    | 否       | 空             | 参考图列表。支持 URL 或 Base64（**不可带 data: 前缀**）      |
| `element_list`     | array    | 否       | 空             | **主体参考列表**。基于主体库中的 `element_id` 配置           |
| `resolution`       | string   | 否       | 1k             | 清晰度：`1k` (标清), `2k` (高清)                             |
| `aspect_ratio`     | string   | 否       | auto           | 纵横比：`16:9`, `9:16`, `1:1`, `4:3`, `3:4`, `3:2`, `2:3`, `21:9`, `auto` |
| `n`                | int      | 否       | 1              | 生成图片数量 [1, 9]                                          |
| `external_task_id` | string   | 否       | 无             | 用户自定义任务 ID，需保证唯一性                              |
| `callback_url`     | string   | 否       | 无             | 任务结果回调地址                                             |

> **💡 核心逻辑说明：**
>
> - **多图引用**：在 `prompt` 中通过 `让<<<image_1>>>和<<<image_2>>>坐在一起吃火锅` 的方式实现多主体融合。
> - **数量限制**：参考主体（element）数量与参考图片（image）数量之和**不得超过 10 个**。
> - **智能比例**：当 `aspect_ratio` 为 `auto` 时，模型将根据传入内容智能决定输出尺寸。

------

## 2. 查询任务（单个）

- **请求地址**: `GET /v1/images/omni-image/{task_id}`

### 响应体

JSON

```
{
  "code": 0,
  "message": "string",
  "request_id": "string",
  "data": {
    "task_id": "string",
    "task_status": "succeed", // submitted, processing, succeed, failed
    "task_status_msg": "string", // 失败原因
    "task_info": { "external_task_id": "string" },
    "task_result": {
      "images": [
        {
          "index": 0,
          "url": "https://..." // 生成图片的 URL，有效期 30 天
        }
      ]
    },
    "created_at": 1722769557708,
    "updated_at": 1722769557708
  }
}
```

------

## 3. 查询任务（列表）

- **请求地址**: `GET /v1/images/omni-image`
- **查询参数**:
  - `pageNum`: 页码 [1, 1000]
  - `pageSize`: 每页数量 [1, 500]

### 响应体

JSON

```
{
  "code": 0,
  "data": [
    {
      "task_id": "string",
      "task_status": "succeed",
      "task_info": { "external_task_id": "string" },
      "task_result": {
        "images": [{ "index": 0, "url": "..." }]
      },
      "created_at": 1722769557708,
      "updated_at": 1722769557708
    }
  ]
}
```

------

